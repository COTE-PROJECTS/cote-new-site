/** @type {import('next').NextConfig} */
const nextConfig = {
    // add this host www.unwantedwitness.org
    images: {
        domains: ['www.unwantedwitness.org', 'cdni.iconscout.com', 'downloadscdn6.freepik.com', 'api1.iloveimg.com', 'api10.iloveimg.com'],
    },
};

export default nextConfig;
